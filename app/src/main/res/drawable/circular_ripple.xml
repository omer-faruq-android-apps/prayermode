<!-- res/drawable/circular_ripple.xml -->
<ripple xmlns:android="http://schemas.android.com/apk/res/android"
    android:color="?attr/colorControlHighlight"> <!-- Uses system ripple color -->

    <item android:id="@android:id/mask">
        <shape android:shape="oval">
            <solid android:color="@android:color/white"/> <!-- Mask color doesn't affect visual -->
            <size android:width="48dp" android:height="48dp"/>
        </shape>
    </item>
</ripple>